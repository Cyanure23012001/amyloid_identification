 
import os

# create modified_structures folder
if not os.path.exists('modified_structures'):
    os.makedirs('modified_structures')

# create download_folder folder
if not os.path.exists('download_folder'):
    os.makedirs('download_folder')

# create pickle_models folder
if not os.path.exists('pickle_models'):
    os.makedirs('pickle_models')

# create download_folder_assembly folder
if not os.path.exists('download_folder_assembly'):
    os.makedirs('download_folder_assembly')


